<!DOCTYPE html>
<html lang="en">


<% if(locals.header && header == 2) { %> 
  <!-- option 2: header -->  
  <%- include("partials/header-setup-locals/header2") %>
<% } else { %>
  <!-- option 1: header -->
  <%- include("partials/header-setup-locals/setup-locals") %>
  <%- include("partials/header-setup-locals/header1") %>
<% }%>
<body>
  <!-- Navbar will adapt to whether logged in or not -->
  <!-- narbar -->
  <%if(navBars == "CLIENTS"){%>
    <%- include("partials/navigation/navbar-clients") %>
  <%}%>
  <%if(navBars == "OPERATORS"){%>
    <%- include("partials/navigation/navbar-operations") %>
  <%}%>
  <!-- Body either server-side renders one of the react applications App or Databases, or a Login or Register form -->
  <%- body %>
</body>
</html>